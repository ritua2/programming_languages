/*
* rules.c
* this is a multi-line comment */
#include <stdio.h>
int main(){

  printf("%.20f", 1/3.);

printf("Braces come in pairs."); printf("Comment tokens come in pairs."); printf("All statements end with semicolon."); printf("Every program has a main function."); printf("C is done mostly in lower-case."); return 0;
}
